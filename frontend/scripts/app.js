/* P0-T02 entry point. Rendering starts with P1. */
(function () {
  "use strict";

  const app = document.getElementById("app");

  if (!app) {
    throw new Error("Serein application mount point is missing.");
  }
}());
