services:
  web:
    image: caddy:2-alpine
    ports:
      - "127.0.0.1:8088:80"
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./frontend:/srv:ro
    restart: unless-stopped
